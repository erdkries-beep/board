# board

GitHub Pages로 배포되는 개인 대시보드 저장소입니다.

## 저장소 구조

- `index.html` — 배포되는 대시보드 전체. 로그인 화면만 평문이고, 본문은 암호화되어 들어 있습니다.
- `.nojekyll` — Jekyll 처리를 끄기 위한 빈 파일. 건드리지 않습니다.

## 배포 방식

`main` 브랜치가 곧 배포본입니다. push하면 GitHub Pages 빌드가 자동으로 돌아
`https://erdkries-beep.github.io/board/` 에 반영됩니다.

## index.html 암호화 구조

`index.html`은 두 부분으로 되어 있습니다.

1. **평문 로그인 화면** (60줄 남짓) — 비밀번호 입력 폼과 복호화 스크립트
2. **암호화된 본문** — `ENC.ct`에 base64로 들어 있는 대시보드 전체 HTML

암호화는 PBKDF2(SHA-256, 310,000회)로 키를 유도해 AES-GCM으로 수행합니다.
복호화는 사용자가 브라우저에서 비밀번호를 직접 입력할 때 클라이언트 측에서만
일어납니다. 비밀번호는 저장소 어디에도 없습니다.

퍼블릭 저장소이므로 **평문 영역에 실제 데이터가 새어나오지 않는 것이 중요합니다.**

## 대시보드 갱신 절차

사용자는 로컬 세션에서 `index.html`을 새로 만들어 업로드해 줍니다.
받으면 다음 순서로 처리합니다.

### 1. 암호화 상태 확인

평문 영역에 실제 데이터가 없는지 먼저 봅니다.

- 전체 줄 수가 60줄 언저리인지 (본문이 평문이면 훨씬 길어집니다)
- `ENC`에 `salt`, `iv`, `ct`, `iter`가 모두 있고 `iter`가 310000인지
- `ct`를 가린 나머지 평문이 기존 로그인 화면과 같은지
- `noindex, nofollow` 메타 태그가 남아 있는지

평문에 계좌·종목·금액 같은 게 보이면 **커밋하지 말고 사용자에게 알립니다.**

### 2. 최신본인지 확인

`origin/main`을 fetch해서 현재 `index.html`과 비교합니다.
사용자가 GitHub 웹에서 직접 업로드한 커밋("Add files via upload")이
중간에 끼어 있을 수 있습니다.

파일 수정 시각과 원격 커밋 시각을 견줘 어느 쪽이 최신인지 판단하고,
**애매하면 덮어쓰기 전에 사용자에게 먼저 물어봅니다.**

### 3. 커밋 후 확인

`main`에 push하고, Pages 빌드가 success로 끝나는지까지 확인한 뒤 보고합니다.

## 주의사항

- 이 저장소는 **퍼블릭**입니다. 복호화된 대시보드 내용, 비밀번호, 계좌 정보를
  파일이나 커밋 메시지에 절대 남기지 않습니다.
- 커밋 메시지는 `Update dashboard bundle` 정도로 짧게 씁니다.
  무엇이 바뀌었는지 구체적으로 적으면 암호화한 의미가 줄어듭니다.
